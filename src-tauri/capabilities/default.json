{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Local main window and directory picker only",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
